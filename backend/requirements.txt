# --- WAAEM backend (production) ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# data / validation
pydantic==2.10.4
pydantic-settings==2.7.1

# database
SQLAlchemy[asyncio]==2.0.36
aiosqlite==0.20.0          # local dev driver
asyncpg==0.30.0            # PostgreSQL async driver (production)
alembic==1.14.0

# document extraction
pdfplumber==0.11.5
PyMuPDF==1.25.1
python-docx==1.1.2

# AI (Ollama / llama.cpp over HTTP)
httpx==0.28.1

# RAG — vector store, embeddings, crawling, OCR
chromadb==1.5.9
fastembed==0.8.0            # multilingual ONNX embeddings (Arabic + English), CPU
beautifulsoup4==4.15.0
pytesseract==0.3.13         # OCR (requires the `tesseract-ocr` system binary)
pillow==11.0.0
